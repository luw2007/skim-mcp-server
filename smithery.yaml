# Smithery configuration file for TypeScript runtime
# Runtime mode uses npm package instead of custom Docker build

runtime: "typescript"
target: "dist/index.js"

# Environment variables for configuration
env:
  LOG_LEVEL: "info"
  SKIM_MAX_REQUESTS_PER_MINUTE: "10"
  SKIM_MAX_INPUT_SIZE: "10485760"
